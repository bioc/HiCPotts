% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hicpotts_robust_estimation.R
\name{posterior_predictive_hicpotts}
\alias{posterior_predictive_hicpotts}
\title{Conditional posterior-predictive diagnostics for HiCPotts}
\usage{
posterior_predictive_hicpotts(
  fit,
  x_vars = NULL,
  y = NULL,
  dist = "ZINB",
  burnin = NULL,
  n_rep = 100L,
  seed = 1L
)
}
\arguments{
\item{fit}{One fit, a list of independently seeded fits, or block-aware
result from \code{combine_hicpotts_blocks()}.}

\item{x_vars}{Named HiCPotts covariates. May be omitted when a block-aware
fit stored the structured output of \code{process_data()}.}

\item{y}{Observed count matrix. May be omitted when a block-aware fit stored
the structured output of \code{process_data()}.}

\item{dist}{One of \code{Poisson}, \code{NB}, \code{ZIP} or \code{ZINB}.}

\item{burnin}{Initial draws to discard; defaults to half of each chain.}

\item{n_rep}{Number of replicated matrices.}

\item{seed}{Integer simulation seed.}
}
\value{
A list with replicate discrepancies, interval summaries and observed
matrix summaries.
}
\description{
Generates replicated count matrices from posterior parameter draws,
conditional on each chain's final internal (unforced) latent allocation.
It reports cellwise log-count error, neighbour-correlation error,
diagonal-decay error, overall and near-diagonal zero-fraction error, and
variance-to-mean error. The reported forced-zero \code{z_final} is deliberately
not used for these parameter diagnostics.
}
\examples{
N <- 6
y <- matrix(rpois(N * N, 4), N, N)
mk <- function() list(matrix(runif(N * N), N, N))
x_vars <- list(distance = mk(), GC = mk(), TES = mk(), ACC = mk())
fits <- run_chain_betas(
    N = N, iterations = 5, x_vars = x_vars, y = y,
    dist = "ZINB", theta_start = 0.5, size_start = c(1, 1, 1),
    n_chains = 2, seeds = 1:2, robust = FALSE
)
pp <- posterior_predictive_hicpotts(
    fits, x_vars, y,
    dist = "ZINB", n_rep = 5
)
pp$summary
}
