% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hicpotts_diagnostics_and_summaries.R
\name{plot_hicpotts_mcmc_by_component}
\alias{plot_hicpotts_mcmc_by_component}
\title{Plot HiCPotts MCMC traces by component}
\usage{
plot_hicpotts_mcmc_by_component(
  fit,
  index = 1L,
  burnin = NULL,
  component_names = c("Noise", "Signal", "False signal"),
  beta_names = c("Intercept", "Distance", "GC", "TES", "ACC"),
  plot_globals = TRUE,
  plot_size = TRUE,
  ask = interactive()
)
}
\arguments{
\item{fit}{A single HiCPotts fit object or a list of such objects.}

\item{index}{Integer index used when \code{fit} is a list.}

\item{burnin}{Optional burn-in iteration. Defaults to half the iterations.}

\item{component_names}{Character vector of length 3 giving component labels.}

\item{beta_names}{Character vector giving the regression coefficient names.}

\item{plot_globals}{Logical; if \code{TRUE}, also plot \code{gamma} and
\code{theta} when present.}

\item{plot_size}{Logical; if \code{TRUE}, plot the component-wise size
parameters when present.}

\item{ask}{Logical; if \code{TRUE} in an interactive session, pause between
pages.}
}
\value{
Invisibly returns the extracted HiCPotts fit object used for
plotting.
}
\description{
Produces trace plots for the component-specific regression coefficients
and, when available, the size parameters. Optionally also plots the global
parameters \code{gamma} and \code{theta}.

This function is intended for raw fitted HiCPotts objects returned by the
MCMC fitting functions, not for the data frame returned by
\code{compute_HMRFHiC_probabilities()}.
}
\examples{
set.seed(4921)
n_draw <- 12L

make_chain <- function(intercept) {
    cbind(
        rnorm(n_draw, intercept, 0.05),
        rnorm(n_draw),
        rnorm(n_draw),
        rnorm(n_draw),
        rnorm(n_draw)
    )
}

fit <- list(
    chains = list(
        make_chain(-1),
        make_chain(0),
        make_chain(1)
    ),
    size = matrix(5, nrow = 3, ncol = n_draw),
    theta = runif(n_draw, 0.1, 0.2),
    gamma = runif(n_draw, 0.5, 0.8)
)

plot_hicpotts_mcmc_by_component(
    fit,
    burnin = 5,
    ask = FALSE
)

}
