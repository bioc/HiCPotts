% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_chain_unified.R
\name{run_chain_betas}
\alias{run_chain_betas}
\title{Fit one or more Hi-C datasets with HiCPotts}
\usage{
run_chain_betas(
  N,
  x_vars,
  y,
  dist = "ZIP",
  gamma_start = 0.3,
  iterations = 20000L,
  burnin = NULL,
  n_chains = 1L,
  seeds = NULL,
  robust = FALSE,
  initialization = "auto",
  theta_start = NULL,
  size_start = NULL,
  use_data_priors = TRUE,
  user_fixed_priors = NULL,
  gamma_prior_shape = c(1, 1),
  epsilon = NULL,
  abc_potts_sweeps = 0L,
  abc_sim_reps = 4L,
  distance_metric = "manhattan",
  relabel = TRUE,
  mc_cores = 1L,
  verbose = FALSE,
  progress_interval = 50L,
  slope_sd_standardized = 0.5,
  abc_epsilon_quantile = 0.1,
  gamma_update_interval = 5L,
  z_probability_burnin = NULL,
  mcse_stop = TRUE,
  mcse_min_iterations = 10000L,
  mcse_check_interval = 500L,
  mcse_relative_threshold = 0.05,
  diagnostic_control = list(),
  gamma_prior = NULL
)
}
\arguments{
\item{N}{Number of rows and columns in every square interaction matrix.}

\item{x_vars}{Named list containing \code{distance}, \code{GC}, \code{TES} and \code{ACC}.
Entries may be matrices for one dataset or lists of matrices matching \code{y}.}

\item{y}{One numeric count matrix, or a list of independently fitted count
matrices.}

\item{dist}{One of \code{"Poisson"}, \code{"NB"}, \code{"ZIP"} or \code{"ZINB"}.}

\item{gamma_start}{Initial value of the Potts spatial parameter. This is a
starting value, not the parameters of its Beta prior.}

\item{iterations}{Positive maximum MCMC updates per chain.}

\item{burnin}{Initial iterations excluded by robust diagnostics. \code{NULL} uses
half the requested iterations, capped at 5,000.}

\item{n_chains}{Positive number of independently seeded chains per dataset.
The default of one preserves the historical \code{run_chain_betas()} result; four
or more chains are recommended when convergence will be assessed.}

\item{seeds}{Optional integer vector with exactly \code{n_chains} values. When
omitted, \code{seq_len(n_chains)} is used; when \code{n_chains} is omitted, the length
of \code{seeds} determines it.}

\item{robust}{Logical. If \code{TRUE}, return the diagnostic and mode-screened
robust workflow; if \code{FALSE}, return raw fitted chains.}

\item{initialization}{Starting allocation strategy. \code{"auto"} uses two
likelihood-informed starts followed by distance-adjusted and
noise-anchored-random starts, recycled over the requested chains. A single
method is used for every chain; alternatively supply exactly one method per
chain. Available methods are \code{"likelihood_informed"}, \code{"count_quantile"},
\code{"distance_adjusted"}, \code{"noise_anchored_random"} and \code{"random"}.}

\item{theta_start}{Optional starting zero-inflation probability.}

\item{size_start}{Optional three-element vector of starting NB2 dispersions.}

\item{use_data_priors}{Whether to estimate empirical-Bayes regression-prior
parameters. With \code{robust = TRUE}, a multi-chain pilot constructs one prior
that is shared and frozen across all production chains; without the robust
workflow each chain performs its historical warm-up update.}

\item{user_fixed_priors}{Optional fixed regression priors used when
\code{use_data_priors = FALSE}.}

\item{gamma_prior_shape}{Positive two-element numeric vector containing the
shape parameters of the Beta prior on gamma. The default \code{c(1, 1)} is
uniform on \verb{(0, 1)}.}

\item{epsilon}{Optional fixed positive ABC bandwidth. \code{NULL} calibrates one
shared bandwidth for all chains of a dataset.}

\item{abc_potts_sweeps}{Non-negative number of Gibbs sweeps used to
equilibrate each simulated auxiliary Potts field. Zero selects the
lattice-dependent default, \code{max(100, 100 * N^2 / 400)}, which keeps the
auxiliary fields equally well equilibrated as \code{N} grows. That default
makes the ABC step scale as \code{N^4} and dominates run time on large
lattices: at \code{N = 100} it is 2500 sweeps. A smaller value is much
faster but under-equilibrates the auxiliary fields, which biases the
estimated spatial coupling upward.}

\item{abc_sim_reps}{Positive number of auxiliary fields averaged per ABC
proposal. Cost is linear in this value; fewer replicates give a noisier
acceptance decision for the spatial coupling.}

\item{distance_metric}{Retained for source compatibility and ignored.}

\item{relabel}{Whether to apply the model-based component relabelling rule.}

\item{mc_cores}{Positive number of chains to execute concurrently.}

\item{verbose}{Logical. Print sampler progress when \code{TRUE}.}

\item{progress_interval}{Positive number of iterations between progress
messages when \code{verbose = TRUE}.}

\item{slope_sd_standardized}{Standardized slope prior SD used when fixed
priors are constructed automatically by the robust workflow.}

\item{abc_epsilon_quantile}{Quantile in \verb{(0, 1)} used to calibrate the
Gaussian ABC bandwidth when \code{epsilon = NULL}.}

\item{gamma_update_interval}{Positive number of iterations between ABC gamma
updates. Larger values reduce computation but produce fewer gamma moves.}

\item{z_probability_burnin}{Iteration after which latent-state membership
frequencies are accumulated. \code{NULL} uses \code{burnin}.}

\item{mcse_stop}{Whether the sampler may stop early after satisfying the
relative Monte Carlo standard-error rule for every monitored parameter.}

\item{mcse_min_iterations}{Minimum completed iterations before MCSE stopping
is considered.}

\item{mcse_check_interval}{Positive number of iterations between MCSE checks.}

\item{mcse_relative_threshold}{Maximum batch-means MCSE divided by posterior
standard deviation required for early stopping.}

\item{diagnostic_control}{Named list overriding robust diagnostic defaults.
Recognised entries are \code{minimum_component_cells}, \code{minimum_ess},
\code{maximum_rhat}, \code{gamma_boundary_tolerance},
\code{maximum_gamma_boundary_fraction}, \code{minimum_gamma_unique},
\code{mode_agreement_threshold} and \code{minimum_mode_chains}. These settings affect
reliability reporting and mode selection, not the sampler target.}

\item{gamma_prior}{Deprecated compatibility alias for \code{gamma_start}. New
code should use \code{gamma_start}.}
}
\value{
For one matrix, a \code{hicpotts_robust_fit} when \code{robust = TRUE} or a
list of fitted chains otherwise. For a list of matrices, a named list with
one such result per dataset. For backward compatibility, a non-robust fit
of a list with \code{n_chains = 1} is returned as one flat fit per dataset.
}
\description{
\code{run_chain_betas()} is the single public fitting entry point. It can run a
user-selected number of independently seeded chains for one matrix or for
each matrix in a list. With \code{robust = TRUE}, it also constructs priors,
checks covariates, screens replicated allocation modes and applies the
parameter-reliability gates. With \code{robust = FALSE}, it returns the raw
relabelled chains. These choices change orchestration and reporting, not the
fitted likelihood or sampler target.
}
\details{
Initialisation, progress reporting, MCSE stopping, gamma-update frequency,
ABC accuracy/workload and diagnostic thresholds are public controls. The
warm-up tempering schedule, component-defining relationship prior,
component-2/3 barrier and reversible allocation-move settings remain fixed
to preserve the package's component definitions and default sampler.

\code{abc_potts_sweeps}, \code{abc_sim_reps} and \code{gamma_update_interval} are the main
ABC cost controls. Their defaults reproduce the package workflow; reducing
sweeps or replicates trades auxiliary-simulation fidelity for speed, while
increasing the update interval reduces the number of gamma transitions.
}
\examples{
N <- 5
y <- matrix(rpois(N * N, 4), N, N)
mk <- function() matrix(runif(N * N), N, N)
x_vars <- list(distance = mk(), GC = mk(), TES = mk(), ACC = mk())
fits <- run_chain_betas(
    N = N, x_vars = x_vars, y = y, dist = "Poisson",
    iterations = 5, n_chains = 2, robust = FALSE
)
length(fits)
}
